<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.travel.seoul.mapper.QnAMapper">
 	<insert id="qnainsert" parameterType="com.travel.seoul.vo.QnAVO">
 		insert into qnadb(q_id, q_title, q_content,q_date, q_answerstatus) values
 		(#{q_id}, #{q_title}, #{q_content},now(), "답변대기")
 	</insert>
 	<select id="qnalist" resultType="com.travel.seoul.vo.QnAVO">
 		select * from qnadb	
 	</select>
 	<select id="findByID" resultType="com.travel.seoul.vo.QnAVO">
 		select * from qnadb	where q_id = #{q_id} and q_title = #{q_title}
 	</select>
</mapper>